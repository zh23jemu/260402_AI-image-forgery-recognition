#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""
在当前终稿基础上，继续补充约 2000 字正文内容。

注意事项：
1. 只替换指定正文段落的文字内容，不修改段落样式、图片、表格与整体版式。
2. 默认先生成一个新的备份文件，便于用户随时回退。
3. 本脚本基于当前终稿段落索引编写，只适用于本项目现有这份论文终稿。
"""

from __future__ import annotations

import argparse
import re
import shutil
from pathlib import Path

from docx import Document


# 这一批段落扩写总计约增加 2000 字，主要分布在：
# 绪论、相关研究现状、实验设计与实现、实验结果与讨论、结论。
EXPAND_REPLACEMENTS: dict[int, str] = {
    75: (
        "近年来，GAN、扩散模型与多模态生成系统持续提升了伪造图像的真实感，图像检测任务已由寻找明显伪迹，"
        "转向应对高仿真、跨模型和跨分布场景。随着商业生成工具普及，伪造图像在内容审核、数字取证和信息安全中的风险进一步上升。"
        "尤其是在社交媒体传播、自动化内容生产和低成本编辑工具广泛应用的背景下，伪造图像已经不再局限于研究型样本，"
        "而是可能直接进入新闻传播、商业宣传、平台审核甚至司法取证链路。也正因为如此，图像检测系统不仅要在静态测试集上取得较高准确率，"
        "更要在新生成器出现、分布变化加剧和后处理增强的条件下保持稳定表现。"
    ),
    76: (
        "与早期 GAN 场景不同，高质量扩散图像在全局结构与局部细节上更加自然，传统依赖纹理异常的检测方法因而明显退化。"
        "许多过去有效的低层伪迹，在新一代生成模型中要么被削弱，要么只在局部区域短暂出现，"
        "这迫使研究工作从“找破绽”进一步转向“学表示”和“看泛化”。换言之，当前任务真正困难的地方并不只是伪造图像变得更像真实照片，"
        "而是判别依据本身正在快速迁移。"
    ),
    80: (
        "从理论上看，AI 伪造图像识别不仅是二分类问题，还涉及域泛化、少样本适应、伪相关抑制与可解释分析。"
        "统一复现多条技术路线，有助于判断模型究竟学到了什么。若只看单一指标，很容易把闭集高分误当作方法已经足够可靠；"
        "但从研究角度看，更关键的是模型在面对新生成器、新压缩方式和不同视觉风格时，是否还能依赖相对稳定的证据做出判断。"
        "因此，对多条方法路线进行并列复现和比较，本身就是一种有价值的研究组织方式。"
    ),
    81: (
        "从应用上看，稳定的检测能力可服务于媒体审核、平台治理、新闻核验和数字取证，因此需要兼顾精度、泛化与鲁棒性。"
        "特别是在实际部署条件下，系统经常会遇到来源未知、分辨率不统一、后处理复杂甚至被二次编辑的图像，"
        "这些因素都要求检测模型具备比实验室闭集评测更强的适应性。若模型只能在理想化测试协议下工作，"
        "那么它在真实场景中的实际价值就会被显著削弱。"
    ),
    85: (
        "围绕这一目标，本文主要完成五项工作：梳理相关研究；搭建 GenImage 最小可运行实验环境；复现 FSD 与 Stay-Positive 结果；"
        "整理 ADM 场景下的校准与冲突现象；讨论 LVLM 在复杂样本分析中的作用。具体而言，本文既关注方法层面的定位差异，"
        "也关注工程层面的可运行性、结果可复核性与样本级证据是否能够支撑结论，使论文不仅停留在结果罗列上，"
        "而是形成较完整的问题分析链条。这样的安排有助于把“能跑通”“能比较”“能解释”三个层面有机衔接起来。"
    ),
    86: (
        "研究方法上，本文采用文献分析、代码复现、结果比较和方案设计相结合的方式推进。"
        "对于每条技术路线，均先确认其公开资源是否可运行，再据已有结果判断其在本项目中的合理定位，"
        "而不是一开始就把所有方法都当作必须完整重训的对象。这样做的原因在于，毕业论文阶段更需要形成清晰、可信、可回溯的研究闭环，"
        "而不是在时间有限的条件下盲目扩张实验范围。"
    ),
    88: (
        "本文的特点在于：先做实基线，再讨论改进；以公开代码、公开权重和公开数据作为实验基础；"
        "在结果解释上保持保守，不夸大未完成的训练与 LVLM 定量能力。这样的写法虽然不追求“结果看起来最多”，"
        "但更符合毕业论文对真实性、可追溯性和边界清晰度的要求，也更有利于后续继续沿着现有结果稳定推进。"
    ),
    93: (
        "AI 伪造图像检测已从早期显式伪迹分析，逐步发展到深度表征学习、泛化检测和鲁棒判别并行推进的阶段。"
        "早期方法更关注频域异常、压缩痕迹、插值边缘或局部纹理破绽，这些策略在低质量 GAN 图像上往往有效，"
        "但随着扩散模型生成质量显著提升，仅依赖单一伪迹线索已难以满足开放场景需求。当前研究更强调模型是否能够跨生成器迁移，"
        "以及是否能在伪造证据变得隐蔽时仍保持有效判别。"
    ),
    94: (
        "当前研究重点已从“在已知数据上区分真假”转向“面对未知生成器、复杂后处理和分布变化时是否仍能稳定识别”。"
        "这意味着评价标准也在变化，研究者越来越重视模型是否能跨类别迁移、是否会因图像分辨率或内容题材变化而迅速失效，"
        "以及输出分数是否具备可校准性。只有当模型在这些方面都表现相对稳定时，其高精度结果才更具有解释意义。"
    ),
    95: (
        "与此同时，研究者开始反思检测器的判别依据是否可靠，即模型究竟在学习伪造机制，还是在利用数据中的偶然偏差。"
        "如果模型只是记住了某类生成图像常见、但并不稳定的统计特征，那么当新的生成器改变这些分布时，检测性能就会快速下滑。"
        "也正因此，“模型是否学到了真正可迁移的伪造证据”成为当前研究必须回答的问题。"
    ),
    98: (
        "面向固定生成器的检测方法通常把任务建模为标准二分类问题，在训练和测试分布相近时往往能够取得较高指标。"
        "这类方法的优势在于训练目标直接、实现成本较低、闭集表现通常较强，因此在很多公开基准或竞赛环境中仍然具有现实价值。"
        "不过，这种优势往往建立在训练分布和测试分布相对接近的前提之上，一旦评测条件被放宽，其性能稳定性就需要重新审视。"
    ),
    99: (
        "这类方法的局限在于高度依赖训练分布，一旦生成器类型、分辨率或后处理方式发生变化，性能可能快速下降。"
        "很多看似“接近满分”的结果，在跨模型迁移后会出现明显回落，这也是近年来研究者不断强调开放场景评测的重要原因。"
        "因此，闭集指标虽然重要，但不能直接替代对泛化能力的分析。"
    ),
    102: (
        "面向未知生成器的泛化检测方法更强调模型在新类别和新分布下的适应能力，通常结合元学习、少样本学习或跨域表征思想。"
        "与固定生成器方法相比，这一方向更接近真实应用场景，因为实际系统很难预先覆盖所有未来会出现的图像生成器。"
        "同时，这类方法也更容易暴露训练流程复杂、超参数敏感和实现成本较高等问题，因此其工程落地难度通常高于标准闭集分类。"
    ),
    103: (
        "FSD 是这一方向的代表方法，其核心价值在于把 AI 伪造图像识别问题推进到少样本适配与未知生成器场景。"
        "它并不单纯追求在某一固定生成器上拿到最高闭集准确率，而是试图建立一种面向新类别仍可工作的检测范式。"
        "从论文结构上看，FSD 也因此更适合作为后续改进研究的主干基线。"
    ),
    106: (
        "另一条重要路线关注判别依据的可靠性，即避免模型把“真实图像特征”误当作决定性证据。"
        "若模型主要依赖真实图像中常见而伪造图像暂时缺失的线索，那么一旦生成模型补齐这些特征，检测器就可能快速失效。"
        "因此，研究“模型为何做出判断”与“模型依赖了什么证据”，已经成为判断方法是否可信的重要部分。"
        "这类问题虽然不像准确率那样直观，却直接关系到方法是否具有长期有效性。"
    ),
    107: (
        "Stay-Positive 强调通过抑制真实图像特征依赖来提升鲁棒性，其意义不只在于提高指标，更在于纠正错误取证路径。"
        "换句话说，该方法试图减少模型把“像真实照片”误当成唯一判断标准的倾向，而把注意力更多放到伪造样本真正暴露出的异常信息上。"
        "这个思想对本文十分重要，因为它提示我们：泛化能力与判别依据稳定性并不是彼此替代的关系，而是需要同时考虑的两个维度。"
    ),
    110: (
        "LVLM 路线尝试利用视觉编码、语言理解和跨模态推理能力，对复杂场景中的语义不一致与局部异常进行辅助分析。"
        "这类模型不一定在数值上立即优于专门训练的检测器，但它们在面对难样本时能够提供更接近人工检查流程的描述性证据，"
        "例如指出文字区域不自然、局部连接关系异常、器官或物体结构不协调等，从而补足纯分数模型在解释层面的不足。"
        "对于论文写作来说，这类描述性能力也有助于把样本级现象转化为更清晰的研究结论。"
    ),
    111: (
        "这类方法更适合处理边界样本、冲突样本和难以用单一分数解释的复杂案例。"
        "对于论文写作而言，LVLM 的价值也在于帮助把“模型为什么可能错”转化为更可描述、更贴近人工观察语言的分析文本。"
        "当前虽然还不足以把它作为统一评测主模型，但它已经能够在案例分析层面提供有效支持。"
    ),
    115: (
        "GenImage 是当前极具代表性的公开基准之一，覆盖多类生成器，适合同时观察固定判别能力和跨生成器差异。"
        "对于本文而言，它既能支持 FSD 这类泛化检测方法的观察，也能为 Stay-Positive 的固定真假对照实验提供可复核的数据基础。"
        "更重要的是，它为不同路线方法之间建立了一个相对统一的比较语境，使结果讨论更具可解释性。"
    ),
    118: (
        "基于上述研究现状，本文将 FSD、Stay-Positive 与 LVLM 视为解决不同层面问题的三条互补路线，而非简单竞争关系。"
        "这样的定位有助于避免把不同任务目标的方法强行拉到同一标尺上比较，也使论文能够从“主干检测能力”“判别依据稳定性”"
        "“复杂案例解释能力”三个维度展开。"
    ),
    119: (
        "本文的切入点是基于公开资源建立可信的最小复现基线，并围绕正式结果讨论后续可执行的改进路径。"
        "相较于一开始就大规模扩展实验，这种策略更能保证每一步工作都可以落地、可以复查，也更适合当前毕业论文的时间边界。"
        "这样的研究切入点虽然更保守，但更有利于形成真实、清楚、可继续扩展的阶段性成果。"
    ),
    144: (
        "本文实验主要基于公开数据集 GenImage 展开，并围绕 Midjourney、Stable Diffusion、ADM 与 real 类图像构建最小可运行验证环境。"
        "之所以优先采用这一策略，是因为项目当前阶段更需要一套可稳定复现、可逐步扩展的实验底座，而不是在尚未打通链路前就追求大而全的覆盖范围。"
        "通过先把关键链路跑通，再逐步叠加更复杂的分析，能够显著降低后续工作中的不确定性。"
    ),
    146: (
        "在数据组织上，FSD 直接按目录结构读取数据，Stay-Positive 则通过真假对照 CSV 进行验证，两者因此采用不同输入方式。"
        "这种差异也意味着两套实验在工程实现上不能完全共享脚本，需要分别处理数据准备、结果导出和错误排查。"
        "对本文而言，这种差异并非单纯的工程细节，而是直接影响结果可比性和复现效率的重要因素。"
    ),
    152: (
        "FSD 的实验流程包括数据整理、checkpoint 适配、正式评估和 ADM 微调探索。具体执行时，先完成公开权重的兼容转换，"
        "再验证官方基线是否能够在当前环境中稳定跑通，随后围绕 ADM 构造额外训练与评估链路，用于观察该方法在困难类别上的可调性。"
        "整个流程的核心目标不是盲目追求更高数值，而是确保每一步结果都可解释、可复核。"
    ),
    157: (
        "实现过程中主要遇到三类问题：数据组织成本较高、FSD checkpoint 需要兼容性处理，以及部分训练与推理流程对环境配置较敏感。"
        "这些问题虽然看起来偏工程，但实际上会直接影响最终结果是否可信，因此本文在写作中也保留了必要的实现背景说明。"
        "换句话说，工程可运行性并不是附属问题，而是基线复现研究能否成立的前提。"
    ),
    167: (
        "FSD 在六类生成器上均具备一定检测能力，其中 GLIDE 表现最好，Stable Diffusion 次之，而 ADM 与 VQDM 相对较弱。"
        "这说明 FSD 已具备跨生成器识别能力，但不同生成器之间仍存在明显难度差异。换言之，所谓“泛化”在当前结果中并不是均匀成立的，"
        "而是呈现出较强的类别分层现象：对某些生成器可以稳定识别，对另一些生成器则仍然容易出现性能塌陷或分数不稳定。"
        "这也提示我们，后续改进不应只盯住平均指标，而要更加关注不同类别之间的结构性差异。"
    ),
    172: (
        "Stay-Positive 在 Midjourney 与 Stable Diffusion 的固定真假对照测试中表现出很强的直接判别能力，"
        "两种模型的准确率和 AP 都显著高于 FSD 当前结果。这说明在训练和测试协议相对接近、真假对照关系清晰时，"
        "围绕正负判别边界优化的策略能够得到更强的闭集检测性能，也证明其“忽略真实图像特征干扰”的思路具有实际效果。"
        "对于本文的比较框架而言，这一结果也帮助我们进一步明确了 Stay-Positive 更适合作为高性能判别基线的角色。"
    ),
    173: (
        "不过，在 ADM 扩展观察中，Rajan/Ours+ 的真实样本准确率为 99.77%，伪造样本准确率仅为 4.73%，整体准确率为 52.25%，"
        "而 AP 仍达到 87.58%。这表明其问题更像阈值失配与分数分布漂移，而不是完全失去排序能力。也正因为如此，ADM 成为了本文最值得展开分析的关键场景，"
        "因为它能够同时暴露方法在阈值、分布和证据偏好上的差异。"
    ),
    175: (
        "若仅比较当前已获得的正式结果，Stay-Positive 在固定二分类任务中明显优于 FSD；但两类方法的任务定位并不完全一致，"
        "因此不宜简单得出“全面更优”的结论。更合理的做法是把结果放回具体任务背景中理解：一个更擅长闭集高性能判别，"
        "一个更适合作为开放场景研究主干。只有在明确任务目标差异之后，这样的比较才具有真正的研究意义。"
    ),
    179: (
        "围绕 ADM，本文还完成了从零初始化训练、首轮 checkpoint 微调和第二轮保守微调三组探索。"
        "三组实验虽然规模有限，但足以帮助判断当前问题究竟更像“训练链路未打通”，还是“方法本身对该类别高度敏感”。"
        "从最终结果看，后者的解释更具说服力。也就是说，当前困难并不主要来自工程无法运行，而是来自类别本身对方法提出了更高要求。"
    ),
    187: (
        "从整体结果看，本文得到三点启示：基线复现路径是可行的；固定二分类高分并不能替代泛化能力分析；"
        "ADM 是最值得深入研究的方法差异场景。更进一步地说，ADM 不只是一个“更难的类别”，它还像一个放大镜，"
        "把不同方法的取证偏好、阈值依赖性和失败模式都更明显地暴露出来。这种放大效应也正是本文后续围绕样本级冲突开展分析的原因。"
    ),
    190: (
        "更重要的是，ADM 的样本级统计把这种差异从“类别差异”推进到了“失败模式差异”：在 3000 个 ADM 假样本中，"
        "存在 1241 个校准后 Stay-Positive 与三组 FSD 全冲突的样本，其中主导模式“SP=fake;FSD=real/real/real”达到 1137 个。"
        "这个现象说明，两类方法的差异不能再只用单一准确率解释，因为它们在同一批样本上往往关注到了不同证据，"
        "进而形成稳定而非偶然的分歧。也正因此，后续研究需要把样本级现象与类别级指标结合起来理解。"
    ),
    201: (
        "尽管本文已经获得较为明确的结果，但仍存在局限：两类方法的覆盖范围与测试协议并不完全一致，ADM 相关证据也主要集中在单一场景。"
        "此外，当前比较更多体现为“同一项目下的阶段性并行观察”，而不是严格意义上的统一训练预算与统一测试协议对比，"
        "因此在写作中需要保持结论边界。只有清楚承认这些边界，论文中的比较与讨论才更具可信度。"
    ),
    206: (
        "总体上，本文在结果解释上坚持“已完成什么就写什么”的原则，即承认 Stay-Positive 的高性能，也保留 FSD 的研究价值，"
        "并明确 LVLM 主要承担解释和辅助分析任务。这种处理方式虽然相对克制，但更符合当前证据水平，也能避免论文结论与实际完成度之间出现脱节。"
        "对于毕业论文来说，真实、稳健、边界清楚往往比盲目追求更激进的结论更重要。"
    ),
    211: (
        "在继续推进论文之前，仍需面对几个现实问题：实验覆盖范围有限、不同方法协议不完全一致，以及 LVLM 仍缺少统一定量结果。"
        "这些问题并不否定当前工作的价值，但提醒我们后续更适合做证据补强和文本收束，而不是贸然扩大实验规模。"
        "换言之，当前阶段最重要的不是再增加多少实验数量，而是把已有结果组织成更有解释力的研究结论。"
    ),
    215: (
        "基于现有结果，后续总体思路是以 FSD 为主干，吸收 Stay-Positive 的鲁棒判别思想，并把 LVLM 作为复杂样本解释接口纳入整体方案。"
        "若后续继续推进代码工作，最合理的方向不是重新铺开大量新实验，而是围绕 ADM 这类高价值困难场景做更聚焦的误判分析、"
        "轻量校准和结构化案例总结。这样既能控制风险，也能让研究持续沿着最有价值的问题推进。"
    ),
    220: (
        "从论文写作角度看，较为稳妥的安排是先完成基线复现与结果讨论，再在此基础上补充异常解释和 LVLM 相关分析，"
        "必要时最后再评估是否补充极小规模实验。这一顺序有助于优先锁定已经完成且可复核的成果，也更符合当前项目的实际进度和答辩准备需求。"
    ),
    224: (
        "本文围绕未知生成器泛化、鲁棒判别与复杂样本解释三个方向，对 FSD、Stay-Positive 与 LVLM 开展了基线复现与比较分析。"
        "论文重点并非宣称提出一套全新最终模型，而是在公开资源条件下把关键基线跑通、把结果讲清楚、把方法边界说明白。"
        "这种处理方式虽然更克制，却更能体现阶段性研究工作的真实价值。"
    ),
    225: (
        "结果表明，FSD 在六类生成器上表现出明显分层，Stay-Positive 在固定真假对照任务中更强，而 ADM 场景则同时暴露出校准问题与稳定的样本级互补性。"
        "尤其是 Rajan/Ours+ 在 ADM 上默认阈值下仅得 52.25%，经 0.388818 校准后可提升到 80.77%；同时，3000 个 ADM 假样本中存在 1137 个"
        "“Stay-Positive 判 fake 而 FSD 三组均判 real”的主导互补样本。训练探索还说明，基于官方 checkpoint 的微调明显优于随机初始化，"
        "但更保守的第二轮微调未继续改善 ADM。综合这些结果可以看到，方法差异并不只体现在总指标上，更体现在对困难样本的关注方式与证据偏好上。"
    ),
    226: (
        "综合来看，Stay-Positive 更适合作为高性能判别基线，FSD 更适合作为后续泛化检测研究主干，LVLM 更适合作为复杂样本的语义分析补充。"
        "本文已经完成了从基线复现到样本级分析的关键过渡，并为后续最小改动改进实验提供了基础。对于毕业论文而言，"
        "这样的组织方式也更稳妥：既保留真实完成的实验成果，又为未来继续深化 FSD 主干和引入 LVLM 分析留下清晰入口。"
        "后续若有更多时间和资源，围绕 ADM 的误判模式继续做有控制的深化，将是最具性价比的研究方向。"
    ),
}


def char_count(text: str) -> int:
    """按当前论文使用的口径统计去空白后的字符数。"""

    return len(re.sub(r"\s+", "", text))


def set_paragraph_text(paragraph, text: str) -> None:
    """
    只替换段落文字，尽量保留原段落格式。

    这里沿用与前面文档脚本一致的策略：记录首个 run 的常见字体属性，
    清空原 runs 后写入新的正文内容，从而尽量减少对用户手调格式的影响。
    """

    first_run = paragraph.runs[0] if paragraph.runs else None
    run_props = {}
    if first_run is not None:
        run_props = {
            "bold": first_run.bold,
            "italic": first_run.italic,
            "underline": first_run.underline,
            "font_name": first_run.font.name,
            "font_size": first_run.font.size,
        }

    for run in list(paragraph.runs):
        paragraph._p.remove(run._r)

    new_run = paragraph.add_run(text)
    if run_props:
        new_run.bold = run_props["bold"]
        new_run.italic = run_props["italic"]
        new_run.underline = run_props["underline"]
        new_run.font.name = run_props["font_name"]
        new_run.font.size = run_props["font_size"]


def parse_args() -> argparse.Namespace:
    """解析命令行参数。"""

    parser = argparse.ArgumentParser(description="在论文终稿基础上再补约 2000 字正文。")
    parser.add_argument("--input", required=True, help="待处理的 DOCX 路径。")
    parser.add_argument(
        "--backup",
        help="备份文件路径；若不提供，则自动生成“_加字前备份”文件。",
    )
    return parser.parse_args()


def main() -> None:
    """执行正文扩写，并输出处理前后字数。"""

    args = parse_args()
    input_path = Path(args.input)
    if not input_path.exists():
        raise FileNotFoundError(f"未找到文件：{input_path}")

    backup_path = (
        Path(args.backup)
        if args.backup
        else input_path.with_name(f"{input_path.stem}_加字前备份{input_path.suffix}")
    )
    if not backup_path.exists():
        shutil.copy2(input_path, backup_path)

    doc = Document(str(input_path))
    before = sum(char_count(p.text) for p in doc.paragraphs)

    for idx, text in EXPAND_REPLACEMENTS.items():
        if idx >= len(doc.paragraphs):
            raise IndexError(f"段落索引超出范围：{idx}")
        set_paragraph_text(doc.paragraphs[idx], text)

    after = sum(char_count(p.text) for p in doc.paragraphs)
    doc.save(str(input_path))

    print(f"backup: {backup_path}")
    print(f"before_chars: {before}")
    print(f"after_chars: {after}")


if __name__ == "__main__":
    main()
